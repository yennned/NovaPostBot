"""E2E-харнесс: прогон настоящих `Update` через настоящий `build_dispatcher`.

Зачем отдельный инструмент, а не тесты: юнит-вызов хендлера скрывает и ошибки
сигнатуры, и падения в теле — aiogram пишет трейс в лог и **молча ничего не
отвечает**, то есть для пользователя «кнопка не реагирует». Такие дефекты
находились только прогоном живого `Update` (`802022a`, `d964aee`, `78d2453`).

Состав:
- `env.py`      — загрузка окружения стенда (`.env.prod`) до импорта `app.*`;
- `lib.py`      — `RecordingBot`, `Screen`, `Persona` (тап по видимому тексту кнопки);
- `human.py`    — «человеческий шум»: опечатки, двойные тапы, откаты, мусор;
- `preflight.py`— доступы, валидация ключа НП, срез «до»;
- `validate.py` — единый отчёт по всем логам + уборка.
"""
